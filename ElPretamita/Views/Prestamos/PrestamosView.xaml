<UserControl x:Class="ElPretamita.Views.Prestamos.PrestamosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="700" d:DesignWidth="1000"
             Background="Transparent"
             Loaded="UserControl_Loaded">

    <UserControl.Resources>
        <!-- Animaciones para el Modal/Overlay fluido -->
        <Storyboard x:Key="FadeInModal">
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ModalOverlay" Storyboard.TargetProperty="Visibility">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Visible}"/>
            </ObjectAnimationUsingKeyFrames>
            <DoubleAnimation Storyboard.TargetName="ModalOverlay" Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ModalPanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="0.8" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ModalPanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="0.8" To="1" Duration="0:0:0.3">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseOut"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
        </Storyboard>

        <Storyboard x:Key="FadeOutModal">
            <DoubleAnimation Storyboard.TargetName="ModalOverlay" Storyboard.TargetProperty="Opacity" From="1" To="0" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseIn"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ModalPanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" From="1" To="0.9" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseIn"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="ModalPanel" Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" From="1" To="0.9" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction><CubicEase EasingMode="EaseIn"/></DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <ObjectAnimationUsingKeyFrames Storyboard.TargetName="ModalOverlay" Storyboard.TargetProperty="Visibility" BeginTime="0:0:0.2">
                <DiscreteObjectKeyFrame KeyTime="0:0:0" Value="{x:Static Visibility.Collapsed}"/>
            </ObjectAnimationUsingKeyFrames>
        </Storyboard>

        <!-- Estilo base de Botones en esta vista -->
        <Style x:Key="ActionBtnStyle" TargetType="Button">
            <Setter Property="Background" Value="#10B981"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="15,10"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" Background="{TemplateBinding Background}" CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#34D399"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Estilo DataGrid Oscuro -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderBrush" Value="#2D2D3F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="RowBackground" Value="#1E1E2E"/>
            <Setter Property="AlternatingRowBackground" Value="#161622"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="RowHeight" Value="40"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="SelectionMode" Value="Single"/>
            <Setter Property="SelectionUnit" Value="FullRow"/>
        </Style>
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#0F172A"/>
            <Setter Property="Foreground" Value="#94A3B8"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,10"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#10B981"/>
        </Style>
        <Style TargetType="DataGridRow">
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <DataTrigger Binding="{Binding RiesgoDeMora}" Value="True">
                    <Setter Property="Background" Value="#7F1D1D"/>
                </DataTrigger>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#10B981"/>
                    <Setter Property="Foreground" Value="White"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#2D2D3F"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="10,0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Padding="{TemplateBinding Padding}" Background="Transparent">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        </Style>
    </UserControl.Resources>

    <Grid>
        <!-- CONTENIDO PRINCIPAL -->
        <Grid Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Cabecera y botón de volver -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <Button Width="40" Height="40" HorizontalAlignment="Left" Background="Transparent" BorderThickness="0" Cursor="Hand" Click="BtnVolver_Click">
                    <TextBlock Text="←" FontSize="24" Foreground="#94A3B8" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="0,-5,0,0"/>
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" CornerRadius="20">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                            <ControlTemplate.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2D2D3F"/>
                                    <Setter Property="Foreground" Value="White"/>
                                </Trigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
                
                <TextBlock Text="Gestión de Préstamos" Foreground="#10B981" FontSize="28" FontWeight="Bold" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Button Style="{StaticResource ActionBtnStyle}" Content="Clientes" Background="#475569" Margin="0,0,10,0" Click="BtnInactivos_Click"/>
                    <Button Style="{StaticResource ActionBtnStyle}" Content="Nuevo Préstamo" Click="BtnNuevoPrestamo_Click"/>
                </StackPanel>
            </Grid>

            <!-- Área de Datos (Dos columnas: Lista y Detalles) -->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/> <!-- Separador -->
                    <ColumnDefinition Width="320"/>
                </Grid.ColumnDefinitions>

                <!-- Columna Izquierda: DataGrid -->
                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <Border Grid.Row="0" Background="#1E1E2E" CornerRadius="8" Padding="10,5" Margin="0,0,0,10">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔍" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="TxtBuscarPrestamo" Background="Transparent" Foreground="White" BorderThickness="0" Width="300" VerticalAlignment="Center" TextChanged="TxtBuscarPrestamo_TextChanged" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="1" Background="#1E1E2E" CornerRadius="10" ClipToBounds="True">
                        <DataGrid x:Name="DgPrestamos" SelectionChanged="DgPrestamos_SelectionChanged">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                            <DataGridTextColumn Header="Nombre del Cliente" Binding="{Binding Cliente.Nombre}" Width="*"/>
                            <DataGridTextColumn Header="Monto Original" Binding="{Binding Monto, StringFormat=C}" Width="110"/>
                            <DataGridTextColumn Header="Cuotas" Binding="{Binding CantidadCuotas}" Width="70"/>
                            <DataGridTextColumn Header="Balance Pendiente" Binding="{Binding BalancePendiente, StringFormat=C}" Width="130"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>
                </Grid>

                <!-- Columna Derecha: Panel de Detalles -->
                <Border Grid.Column="2" Background="#161622" CornerRadius="10" Padding="15">
                    <StackPanel x:Name="PanelDetalles" Visibility="Hidden">
                        <TextBlock Text="Detalles del Préstamo" Foreground="#10B981" FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>
                        
                        <TextBlock Text="Cliente:" Foreground="#94A3B8" FontSize="12"/>
                        <TextBlock x:Name="TxtDetalleNombre" Text="---" Foreground="White" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,10" TextWrapping="Wrap"/>

                        <TextBlock Text="Cédula:" Foreground="#94A3B8" FontSize="12"/>
                        <TextBlock x:Name="TxtDetalleCedula" Text="---" Foreground="White" FontSize="14" Margin="0,0,0,10"/>

                        <Grid Margin="0,0,0,15">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>
                            <StackPanel Grid.Column="0">
                                <TextBlock Text="Monto Original:" Foreground="#94A3B8" FontSize="12"/>
                                <TextBlock x:Name="TxtDetalleMonto" Text="$0.00" Foreground="White" FontSize="14"/>
                            </StackPanel>
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Interés (%):" Foreground="#94A3B8" FontSize="12"/>
                                <TextBlock x:Name="TxtDetalleInteres" Text="0%" Foreground="White" FontSize="14"/>
                            </StackPanel>
                        </Grid>

                        <TextBlock Text="Balance Pendiente:" Foreground="#94A3B8" FontSize="12" Margin="0,5,0,0"/>
                        <TextBlock x:Name="TxtDetalleBalance" Text="$0.00" Foreground="#F59E0B" FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

                        <!-- Botones de Acción sobre el Préstamo seleccionado -->
                        <Button Style="{StaticResource ActionBtnStyle}" Content="Pagar Cuota" Background="#3B82F6" Margin="0,0,0,10" Click="BtnAbonarCuota_Click"/>
                        <Button Style="{StaticResource ActionBtnStyle}" Content="Ver Calendario Pagos" Background="#475569" Margin="0,0,0,10"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>

        <!-- CAPA SUPERPUESTA (OVERLAY) PARA FORMULARIOS FLUÍDOS -->
        <Grid x:Name="ModalOverlay" Background="#99000000" Visibility="Collapsed" Opacity="0">
            <Border x:Name="ModalPanel" Background="#1E1E2E" CornerRadius="15" HorizontalAlignment="Center" VerticalAlignment="Center" Width="400" Padding="20" RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
                </Border.RenderTransform>
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>
                
                <StackPanel>
                    <TextBlock x:Name="TxtModalTitulo" Text="Otorgar Nuevo Préstamo" Foreground="#10B981" FontSize="20" FontWeight="Bold" Margin="0,0,0,20"/>
                    
                    <TextBlock Text="Buscar Cliente Existente (Opcional)" Foreground="#94A3B8" Margin="0,0,0,5"/>
                    <ComboBox x:Name="CmbBuscarCliente" Height="35" Margin="0,0,0,15" IsEditable="True" IsTextSearchEnabled="True" TextSearch.TextPath="Nombre" DisplayMemberPath="Nombre" SelectionChanged="CmbBuscarCliente_SelectionChanged" VerticalContentAlignment="Center" Padding="5,0"/>

                    <TextBlock Text="Nombre del Cliente" Foreground="#94A3B8" Margin="0,0,0,5"/>
                    <TextBox x:Name="TxtFormNombre" Background="#161622" Foreground="White" BorderBrush="#2D2D3F" BorderThickness="1" Padding="10" Height="35" Margin="0,0,0,15"/>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Cédula" Foreground="#94A3B8" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtFormCedula" Background="#161622" Foreground="White" BorderBrush="#2D2D3F" BorderThickness="1" Padding="10" Height="35" TextChanged="TxtFormCedula_TextChanged" MaxLength="13"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Teléfono" Foreground="#94A3B8" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtFormTelefono" Background="#161622" Foreground="White" BorderBrush="#2D2D3F" BorderThickness="1" Padding="10" Height="35" TextChanged="TxtFormTelefono_TextChanged" MaxLength="12"/>
                        </StackPanel>
                    </Grid>

                    <Grid Margin="0,0,0,15">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Monto a Prestar ($)" Foreground="#94A3B8" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtFormMonto" Background="#161622" Foreground="White" BorderBrush="#2D2D3F" BorderThickness="1" Padding="10" Height="35" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
                        </StackPanel>
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Interés Mensual (%)" Foreground="#94A3B8" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtFormInteres" Background="#161622" Foreground="White" BorderBrush="#2D2D3F" BorderThickness="1" Padding="10" Height="35" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
                        </StackPanel>
                    </Grid>

                    <StackPanel Margin="0,0,0,15">
                        <TextBlock Text="Cantidad de Cuotas (Meses)" Foreground="#94A3B8" Margin="0,0,0,5"/>
                        <TextBox x:Name="TxtFormCuotas" Background="#161622" Foreground="White" BorderBrush="#2D2D3F" BorderThickness="1" Padding="10" Height="35" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
                    </StackPanel>

                    <!-- Tipo y Valor de Mora -->
                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="15"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Tipo de Mora" Foreground="#94A3B8" Margin="0,0,0,5"/>
                            <ComboBox x:Name="CmbTipoMora" Background="#161622" Foreground="Black" BorderBrush="#2D2D3F" BorderThickness="1" Height="35" Padding="5">
                                <ComboBoxItem Content="Monto Fijo ($)" IsSelected="True"/>
                                <ComboBoxItem Content="Porcentaje (%)"/>
                            </ComboBox>
                        </StackPanel>

                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Valor de Mora" Foreground="#94A3B8" Margin="0,0,0,5"/>
                            <TextBox x:Name="TxtFormMora" Background="#161622" Foreground="White" BorderBrush="#2D2D3F" BorderThickness="1" Padding="10" Height="35" Text="0" GotFocus="TextBox_GotFocus" LostFocus="TextBox_LostFocus"/>
                        </StackPanel>
                    </Grid>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="Cancelar" Background="#475569" Foreground="White" Padding="10" FontWeight="Bold" Cursor="Hand" BorderThickness="0" Click="BtnCerrarModal_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10"/>
                                    </Border>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                        <Button Grid.Column="2" Style="{StaticResource ActionBtnStyle}" Content="Guardar" Background="#10B981" Click="BtnGuardarPrestamo_Click"/>
                    </Grid>
                </StackPanel>
            </Border>
        </Grid>

        <!-- CAPA SUPERPUESTA PARA DIRECTORIO DE CLIENTES -->
        <Grid x:Name="ModalOverlayClientes" Background="#99000000" Visibility="Collapsed" Opacity="0">
            <Border x:Name="ModalPanelClientes" Background="#1E1E2E" CornerRadius="15" HorizontalAlignment="Center" VerticalAlignment="Center" Width="800" Height="500" Padding="20" RenderTransformOrigin="0.5,0.5">
                <Border.RenderTransform>
                    <ScaleTransform ScaleX="0.8" ScaleY="0.8"/>
                </Border.RenderTransform>
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="0" Opacity="0.5"/>
                </Border.Effect>
                
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Row="0" Margin="0,0,0,15">
                        <TextBlock Text="Directorio de Clientes" Foreground="#10B981" FontSize="20" FontWeight="Bold"/>
                        <Button Content="✕" Background="Transparent" Foreground="#94A3B8" BorderThickness="0" FontSize="18" FontWeight="Bold" HorizontalAlignment="Right" Cursor="Hand" Click="BtnCerrarInactivos_Click">
                            <Button.Template>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Foreground" Value="White"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Button.Template>
                        </Button>
                    </Grid>

                    <Border Grid.Row="1" Background="#1E1E2E" CornerRadius="8" Padding="10,5" Margin="0,0,0,15" HorizontalAlignment="Left">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="🔍" Foreground="#94A3B8" VerticalAlignment="Center" Margin="0,0,10,0"/>
                            <TextBox x:Name="TxtBuscarDirectorio" Background="Transparent" Foreground="White" BorderThickness="0" Width="250" VerticalAlignment="Center" TextChanged="TxtBuscarDirectorio_TextChanged" />
                        </StackPanel>
                    </Border>

                    <Border Grid.Row="2" Background="#161622" CornerRadius="8" ClipToBounds="True">
                        <DataGrid x:Name="DgDirectorioClientes" HeadersVisibility="Column" RowHeight="40" MouseDoubleClick="DgDirectorioClientes_MouseDoubleClick">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Nombre" Binding="{Binding Nombre}" Width="*"/>
                                <DataGridTextColumn Header="Cédula" Binding="{Binding Cedula}" Width="150"/>
                                <DataGridTextColumn Header="Teléfono" Binding="{Binding Telefono}" Width="150"/>
                                <DataGridTemplateColumn Header="Acciones" Width="120">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Ver Historial" Background="#10B981" Foreground="White" BorderThickness="0" Cursor="Hand" Padding="5" Click="BtnVerHistorial_Click">
                                                <Button.Template>
                                                    <ControlTemplate TargetType="Button">
                                                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="{TemplateBinding Padding}"/>
                                                        </Border>
                                                    </ControlTemplate>
                                                </Button.Template>
                                            </Button>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>
